#include <stdio.h>
/**
 * main - program that computes and prints the sum of all
 * the multiples
 *
 * Return: 0
 */
int main(void)
{
	int a, b = 0;

	for (a = 0; a < 1024; a++)
	{
		if ((a % 3) == 0 || (a % 5) == 0)
		{
			b = b + x;
		}
	}
	printf("%d\n", b);
	return (0);
}
